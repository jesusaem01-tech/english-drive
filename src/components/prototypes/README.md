# Prototype Rules

New UI experiments belong in this folder.

Do not replace or modify core learning screens such as `Phase1Listen`, `Phase2Speak`, `Phase3Grammar`, or `HomeScreen` unless the task explicitly asks for that production change.

Prototype screens can use backend utilities and local data, but they should not become the default learning route until the production flow is intentionally integrated.
